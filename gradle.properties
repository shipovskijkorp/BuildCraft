# Shared Gradle settings. Version/loader coordinates live in stonecutter-targets.properties.
org.gradle.jvmargs=-Xmx4G -Dfile.encoding=UTF-8
org.gradle.daemon=false
org.gradle.parallel=true
org.gradle.caching=true

# Optional compatibility runtime controls.
enable_ic2_runtime=true
ci_runtime_profile=default
