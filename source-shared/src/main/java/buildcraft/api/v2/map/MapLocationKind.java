package buildcraft.api.v2.map;

/** Stable semantic kind stored by BuildCraft map-location items. */
public enum MapLocationKind {
    CLEAN,
    SPOT,
    AREA,
    PATH,
    ZONE,
    PATH_REPEATING
}
