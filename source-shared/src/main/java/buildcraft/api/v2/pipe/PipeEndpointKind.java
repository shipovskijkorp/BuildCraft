package buildcraft.api.v2.pipe;

/** Public connection classification without exposing the internal pipe holder/capability implementation. */
public enum PipeEndpointKind {
    NONE,
    PIPE,
    BLOCK
}
