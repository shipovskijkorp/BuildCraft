<lore>
A reliable clock is often more useful than a complicated one.
</lore>
<no_lore>
This trigger pulses once every 10 seconds while a Timer is attached to the pipe.
</no_lore>
<hint>
<bold>Hint:</bold> Connect the trigger to a temporary lamp first. Once its active and inactive states are confirmed, route the same signal to machinery.
</hint>


<chapter name="Timer Pulse"/>
The trigger becomes active on world ticks divisible by 200. Connect it to Gate actions that should repeat at a fixed interval.

<link to="buildcraftsilicon:item/plug_timer"/>
